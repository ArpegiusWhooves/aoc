use std::str::FromStr;

grammar;

pub Num: i32 = <s:r"[0-9]+"> => i32::from_str(s).unwrap();
 
Pair: (i32,i32) = <Num> "," <Num>;

pub List: Vec<(i32,i32)> = { 
    <mut v:(<Pair> "->")*> <e:Pair> => {v.push(e); v}
};
